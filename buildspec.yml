version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - echo Build started on `date`
      - gradlew test
  post_build:
    commands:
      - echo Build completed on `date`
      - java -version
      - pwd
      - ls
      - gradlew build -x test
artifacts:
  files:
    - '**/*'